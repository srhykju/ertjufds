apiVersion: v1
kind: Service
metadata:
  name: dcufhn
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "dcufhn"
    port: 8080
  selector:
    app: dcufhn
